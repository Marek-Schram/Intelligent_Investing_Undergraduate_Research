# Discovery Dossiers (Sleeve E)

One file per candidate: `<TICKER>_<YYYY-MM-DD>.md`.

Every dossier records the full manipulation checklist — **including checks that passed**, because
"we checked and it was clean" is part of the evidence trail — plus the bear case and its three
falsifiers. Committed and never edited; a changed view gets a new dated dossier.

The blank line *"What would have to be true for this to be a mistake:"* is filled in by the
human, before any tranche. Not by the model.
